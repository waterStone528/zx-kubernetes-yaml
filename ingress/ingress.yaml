---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: zx-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: zx-web
                port:
                  number: 80